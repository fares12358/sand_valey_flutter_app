import 'package:flutter/material.dart';

class SeedTypePage extends StatelessWidget {
  const SeedTypePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Seed Type')),
      body: const Center(child: Text('Seed Type Page')),
    );
  }
}

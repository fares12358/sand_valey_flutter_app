import 'package:flutter/material.dart';

class InsecticideTypePage extends StatelessWidget {
  const InsecticideTypePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Insecticide Type')),
      body: const Center(child: Text('Insecticide Type Page')),
    );
  }
}

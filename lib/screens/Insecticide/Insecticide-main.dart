import 'package:flutter/material.dart';

class InsecticideMainPage extends StatelessWidget {
  const InsecticideMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Insecticide Main')),
      body: const Center(child: Text('Insecticide Main Page')),
    );
  }
}

import 'package:flutter/material.dart';

class InsecticideDescriptionPage extends StatelessWidget {
  const InsecticideDescriptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Insecticide Description')),
      body: const Center(child: Text('Insecticide Description Page')),
    );
  }
}

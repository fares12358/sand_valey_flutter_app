import 'package:flutter/material.dart';

class CommunicateEngPage extends StatelessWidget {
  const CommunicateEngPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Communicate English')),
      body: const Center(child: Text('Communicate English Page')),
    );
  }
}

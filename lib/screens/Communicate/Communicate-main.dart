import 'package:flutter/material.dart';

class CommunicateMainPage extends StatelessWidget {
  const CommunicateMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Communicate Main')),
      body: const Center(child: Text('Communicate Main Page')),
    );
  }
}

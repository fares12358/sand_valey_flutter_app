import 'package:flutter/material.dart';

class CommunicateCallPage extends StatelessWidget {
  const CommunicateCallPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Communicate Call')),
      body: const Center(child: Text('Communicate Call Page')),
    );
  }
}

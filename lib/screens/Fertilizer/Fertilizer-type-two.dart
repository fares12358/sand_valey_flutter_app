import 'package:flutter/material.dart';

class FertilizerTypeTwoPage extends StatelessWidget {
  const FertilizerTypeTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fertilizer Type Two')),
      body: const Center(child: Text('Fertilizer Type Two Page')),
    );
  }
}

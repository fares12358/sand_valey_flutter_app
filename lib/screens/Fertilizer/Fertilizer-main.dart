import 'package:flutter/material.dart';

class FertilizerMainPage extends StatelessWidget {
  const FertilizerMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fertilizer Main')),
      body: const Center(child: Text('Fertilizer Main Page')),
    );
  }
}

import 'package:flutter/material.dart';

class FertilizerTypeOnePage extends StatelessWidget {
  const FertilizerTypeOnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fertilizer Type One')),
      body: const Center(child: Text('Fertilizer Type One Page')),
    );
  }
}
